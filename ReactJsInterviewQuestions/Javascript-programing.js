// twoSum  problem
// Your twoSum method will take 3 input parameters
// x=x is an array of integers
// y=y is an array of integers
// target= target is an integer
// return value  will be an array containing 2 integers, xIndex and yIndex,  or an array with 0 integers
// Such that x[xXindex] +y[yYindex]==target
// Exmalple x=[1,2,3] y=[4,5,6,8] target=6.
// Output should be [0, 1], because x[0]+y[1]=1+5==target==6
// Your method should output an array with exactly 2 integers or an array with 0 integers.

// You need find 2 integers, 1 from x array and 1 from y array. Such that
// both the numbers add up to target. The return value will be the indexes of
// the x element and y element

// implement twoSum in the next line

// console.log(twoSum([1, 2, 3, 0], [4, 5, 6, 8], 6));
// console.log(twoSum([1, 2, 3, 0], [4, 5, 6, 8], 8));
// console.log(twoSum([1, 2, 3, 0], [4, 5, 6, 8], 678));
